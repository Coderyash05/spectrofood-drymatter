# Core data + ML (Phase 1 and Phase 3 run on these alone)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
xgboost>=2.0

# Phase 2 only (hyperspectral cube CNN, PyTorch)
# CPU build is fine; install the CUDA build separately if you have a GPU.
torch>=2.0